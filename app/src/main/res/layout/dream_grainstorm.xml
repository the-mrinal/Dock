<?xml version="1.0" encoding="utf-8"?>
<!-- The system screensaver: the wallpaper, and a clock that drifts a few
     pixels a minute so an OLED panel never burns in. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_screen">

    <ImageView
        android:id="@+id/dreamBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0"
        android:scaleType="centerCrop"
        android:contentDescription="@string/background_source_grainstorm" />

    <!-- The drifting group. Its own container so translation never clips the
         wallpaper behind it. -->
    <LinearLayout
        android:id="@+id/dreamClockGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/dreamClock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-thin"
            android:includeFontPadding="false"
            android:shadowColor="#CC000000"
            android:shadowDx="0"
            android:shadowDy="2"
            android:shadowRadius="18"
            android:text="12:00"
            android:textColor="@color/text_primary"
            android:textSize="96sp"
            android:textStyle="bold" />
    </LinearLayout>
</FrameLayout>
